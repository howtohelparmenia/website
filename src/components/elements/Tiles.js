import React from "react"

export const Tiles = ({ children }) => (
  <div className="tiles">
    { children }
  </div>
)

export const Tile = ({ children }) => (
  <div className="tile">
    { children }
  </div>
)
